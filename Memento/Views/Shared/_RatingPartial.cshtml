@model Memento.Models.AlbumViewModel

@if (Model.IsAlbumOfUser)
{
    <span class="rating">@Model.AlbumRating</span>
}
else
{
    <button class="rating-button cons-button"></button>
    <span class="rating">@Model.AlbumRating</span>
    <button class="rating-button pros-button"></button>
}
